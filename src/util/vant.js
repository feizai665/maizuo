import Vue from 'vue'
import { Button, Cell, Icon, List, NavBar, IndexBar, IndexAnchor, Search } from 'vant'

Vue.use(Button).use(NavBar).use(Icon).use(List).use(Cell).use(IndexBar).use(IndexAnchor).use(Search)
